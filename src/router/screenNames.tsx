const stack = 'Stack';
export const STACK_NAME = {
  home: `${stack}-Home`,
  userList: `${stack}-UserList`,
};

const tab = 'Tab';
export const TAB_NAME = {
  home: `${tab}-Home`,
  userList: `${tab}-UserList`,
};
