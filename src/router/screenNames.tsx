const stack = 'Stack';
export const STACK_NAME = {
  home: `${stack}-Home`,
  about: `${stack}-About`,
};

const tab = 'Tab';
export const TAB_NAME = {
  home: `${tab}-Home`,
  about: `${tab}-About`,
};
